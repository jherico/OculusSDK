message(FATAL_ERROR "Do not run CMake in, or against this directory.  " 
    "Instead, create a build directory that is ignored by git our outside the source tree and run cmake against the CMake subdiretory")
